#
# Compass/Sass configuration.
#

css_dir = "css"
sass_dir = "sass"
extensions_dir = "sass-extensions"
images_dir = "img"
javascripts_dir = "js"

# You can select your preferred output style here (can be overridden via the command line):
# output_style = :expanded or :nested or :compact or :compressed
output_style = :expanded

line_comments = false

# To enable relative paths to assets via compass helper functions.
relative_assets = true

# Pass options to sass.
# - For development, use the FireSass-compatible debug_info:
#sass_options = {:debug_info => true}
# - For production, force the CSS to be regenerated even though the source scss
#   may not have changed, since we want the CSS to be compressed and have the
#   debug info removed:
#sass_options = {:always_update => true}
